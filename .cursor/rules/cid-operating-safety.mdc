---
description: Cursor loader for CID / SERVICIOS_CINE operating rules
alwaysApply: true
---

# CID Cursor Operating Safety

Single authoritative source: `AGENTS.md` at the repository root (`/opt/SERVICIOS_CINE/AGENTS.md`).

This file exists only so Cursor always loads that guide. Do not add or maintain a second full copy of operating rules here.

Before any work:
1. Read and obey `AGENTS.md`.
2. Inspect relevant `directivas/` when the task touches architecture, security, or critical flows.
3. Run `git status --short` from `/opt/SERVICIOS_CINE` with the repo `.venv` activated.
