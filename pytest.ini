[pytest]
testpaths = tests
norecursedirs =
    OLD
    ai-dubbing-legal-studio
    .venv
    src/.venv
    node_modules
    dist
    build
pythonpath =
    src
asyncio_mode = auto
markers =
    asyncio: mark test as async
